/**
 * Tipos de evento alinhados a AnalyticsEventType do backend (valores numéricos).
 */
export enum AnalyticsEventType {
  ListingViewed = 1,
  ListingWhatsappClicked = 2,
  StoreViewed = 3,
  StoreWhatsappClicked = 4,
  StoreShared = 5,
}
