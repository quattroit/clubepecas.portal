/** Mesma largura do formulário (`max-w-2xl`) e do input de título. */
export const photoGallerySectionClassName =
  "border-border bg-card flex w-full max-w-2xl flex-col gap-2.5 rounded-xl border p-3 sm:p-4";

/** Três cards iguais ocupando toda a largura da section. */
export const photoGalleryGridClassName =
  "grid w-full grid-cols-3 gap-2.5 sm:gap-3";
